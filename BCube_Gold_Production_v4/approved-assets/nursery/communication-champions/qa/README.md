# Approved Page QA Records

Store the signed JSON QA record for every approved page and reusable illustration here. A QA record must conform to `schemas/qa-record.schema.json` and match the exact candidate SHA-256.

Changing a PNG invalidates its QA record and approval lock.
